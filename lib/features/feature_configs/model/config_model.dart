class ConfigModel {
  String? remake;
  String? address;
  String? delay;
  bool isSelected = false;

  ConfigModel({
    this.remake,
    this.address,
    this.delay,
    this.isSelected = false,
  });
}
